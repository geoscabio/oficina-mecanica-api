# Controla o que a esteira CD Development executa ao entrar na branch develop.
# apply   = cria ou atualiza os recursos AWS gerenciados pelo Terraform.
# destroy = destrói os recursos AWS gerenciados pelo Terraform.
# Execução intencional de destroy para promover o incremento atual de alinhamento do contrato JWT.
TERRAFORM_ACTION=destroy
